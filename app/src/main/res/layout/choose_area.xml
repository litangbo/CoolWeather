<?xml version="1.0" encoding="utf-8"?>
<!--
    遍历全国省市县的功能会多次复用，因此不写在活动里，而是写在碎片里；
    这样在复用的时候，直接在布局里面引用碎片即可。
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:background="#fff"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <!--
        定义一个头布局作为标题栏，高度为ActionBar的高度，背景色为colorPrimary；
        这里之所以自定义标题栏，是因为碎片中最好不要直接使用ActionBar或Toolbar，不然在复用的时候可能会出现意外的效果。
    -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary">
        <TextView
            android:id="@+id/title_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:textColor="#fff"
            android:textSize="20sp"/>
        <Button
            android:id="@+id/back_button"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:layout_marginLeft="10dp"
            android:layout_alignParentLeft="true"
            android:layout_centerVertical="true"
            android:background="@drawable/ic_back"/>
    </RelativeLayout>
    <!--
        省市县的数据将显示在这里；
        之所以使用ListView，是因为它会自动给每一个分项添加一条水平线；
        而使用RecyclerView想实现同样的效果则比较麻烦，这里我们总是选择最优的方案。
    -->
    <ListView
        android:id="@+id/list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>